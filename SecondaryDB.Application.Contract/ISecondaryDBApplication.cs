using SecondaryDB.Domain;

namespace SecondaryDB.Application.Contract;

public interface ISecondaryDBApplication
{
    ////void Create(ProductQuery productQuery);
    ////void Update(ProductQuery productQuery);
    ////void Remove(ProductQuery productQuery);
    ////void Get(long productId); 
}